# Simple RLE Encoder/Decoder

This project provides a straightforward implementation of Run-Length Encoding (RLE), a simple form of lossless data compression where runs of consecutive identical characters are encoded as the character and its count.